nav.navbar.navbar-default
  .container-fluid.container
    .navbar-header
      button.navbar-toggle.collapsed(type='button', data-toggle='collapse', data-target='#bs-example-navbar-collapse-1', aria-expanded='false')
        span.sr-only Toggle navigation
        span.icon-bar
        span.icon-bar
        span.icon-bar
      a.navbar-brand(href='/') Ivonete Online
    #bs-example-navbar-collapse-1.collapse.navbar-collapse.collapse
      ul.nav.navbar-nav
        if current == 'index'
          li.active
            a(href='/')
              | Home
              span.sr-only (current)
        else
          li
            a(href='/')
              | Home
              span.sr-only (current)
        if current == 'my_bills'
          li.active
            a(href='/boletos') Meus Boletos
        else
          li
            a(href='/boletos') Meus Boletos
      ul.nav.navbar-nav.navbar-right
        if current == 'admin_upload'
          a(href='/admin-login')
            button.btn.btn-default.navbar-btn(type='button', style='margin-left: 10px;') Sair
        else
          a(href='/admin-login')
            button.btn.btn-default.navbar-btn(type='button', style='margin-left: 10px;') Entrar como Admin
